# Гурт «Грім та Грім»

## Запуск через npm

```bash
npm install
npm run dev
```

Після запуску відкрийте адресу, яку покаже Vite (зазвичай `http://localhost:5173`).

## Продакшн збірка

```bash
npm run build
npm run preview
```
